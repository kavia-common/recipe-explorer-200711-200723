# FastAPI backend environment variables (example)
# NOTE: Do NOT commit real secrets; values here are for local development only.

# Required: PostgreSQL connection string (same as database_postgresql/db_connection.txt without leading `psql `)
DATABASE_URL=postgresql://appuser:dbuser123@localhost:5000/myapp

# Optional: React frontend origin for CORS (defaults to http://localhost:3000)
FRONTEND_ORIGIN=http://localhost:3000
